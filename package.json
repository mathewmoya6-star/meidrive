{
  "name": "meidriveafrica",
  "version": "1.0.0",
  "description": "MEI Drive Africa - NTSA Approved Driver Training Platform",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.dev.js",
    "build": "node esbuild.build.js",
    "start": "node api/server.js",
    "vercel-build": "node esbuild.build.js",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": "20.x"
  }
}
